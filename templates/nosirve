{% extends "base.html" %}

{% block cards %}
<div class="container-fluid">
    <div class="row row-cols-1 row-cols-md-3 g-4 justify-content-center text-center">
        <div class="col">
            <div class="card h-100 border-0 bg-transparent">
                <img src="{{ url_for('static', filename='img/e.jpeg') }}" class="card-img-top rounded-circle w-75 mx-auto" alt="Agenda" style="aspect-ratio: 1/1; object-fit: cover;">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title fw-bold">Tareas</h5>
                    <p class="card-text text-muted small">Organiza tu horario diario.</p>
                    <div class="mt-auto">
                        <a href="#" class="btn bg-danger-subtle border-danger text-dark w-100 shadow-sm">Ir a Tareas</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="col">
            <div class="card h-100 border-0 bg-transparent">
                <img src="{{ url_for('static', filename='img/h.jpeg') }}" class="card-img-top rounded-circle w-75 mx-auto" alt="Metas" style="aspect-ratio: 1/1; object-fit: cover;">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title fw-bold">Metas</h5>
                    <p class="card-text text-muted small">Rastrea tus objetivos.</p>
                    <div class="mt-auto">
                        <a href="#" class="btn bg-danger-subtle border-danger text-dark w-100 shadow-sm">Ir a Metas</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="col">
            <div class="card h-100 border-0 bg-transparent">
                <img src="{{ url_for('static', filename='img/m.jpeg') }}" class="card-img-top rounded-circle w-75 mx-auto" alt="Personal" style="aspect-ratio: 1/1; object-fit: cover;">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title fw-bold">Calendario</h5>
                    <p class="card-text text-muted small">Los días a la semana</p>
                    <div class="mt-auto">
                        <a href="#" class="btn bg-danger-subtle border-danger text-dark w-100 shadow-sm">Ir a Calendario</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block content %}
    <form action="/agregar" method="POST" class="input-group mb-4 shadow-sm">
        <input type="text" name="tarea" class="form-control border-warning" placeholder="Escribe una nueva tarea..." required>
        <button class="btn btn-danger-subtle fw-bold" type="submit">Agregar</button>
    </form>

    <div class="d-flex flex-column gap-3">
        {% if tareas %}
            {% for tarea in tareas %}
                <div class="p-3 border rounded shadow-sm bg-white d-flex align-items-center justify-content-between">
                    <div class="d-flex align-items-center">
                        <input class="form-check-input mt-0 me-3" type="checkbox" value="" id="check{{ loop.index }}" style="transform: scale(1.3);">
                        <label class="form-check-label fs-5" for="check{{ loop.index }}">
                            {{ tarea.nombre }}
                        </label>
                    </div>
                    
                    <form action="/eliminar-tarea" method="POST" class="m-0">
                        <input type="hidden" name="id" value="{{ loop.index0 }}">
                        <button type="submit" class="btn btn-outline-danger btn-sm border-0">
                            <i class="bi bi-trash"></i> Eliminar
                        </button>
                    </form>
                </div>
            {% endfor %}
        {% else %}
            <p class="text-muted text-center">No hay tareas aún.</p>
        {% endif %}
    </div>
{% endblock %}